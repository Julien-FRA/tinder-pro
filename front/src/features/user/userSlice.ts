// Exemple de slice (si Redux ou Zustand)
export interface UserState {
  id: string;
  name: string;
}

const initialState: UserState = {
  id: '',
  name: '',
};

// Ajouter ici la logique de gestion d'utilisateur
